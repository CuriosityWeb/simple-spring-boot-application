spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./db/Database;AUTO_SERVER=TRUE
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true
